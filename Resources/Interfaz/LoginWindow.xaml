
<Window x:Class="EV03.Wpf.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="420" Width="620" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" Background="{StaticResource BrushBg}">
  <Grid>
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
      <Border Style="{StaticResource Card}" Width="380">
        <StackPanel>
          <TextBlock Text="Login" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,12"/>
          <StackPanel Margin="0,6,0,0">
            <TextBlock Text="Usuario"/>
            <TextBox x:Name="UserBox" />
          </StackPanel>
          <StackPanel Margin="0,6,0,0">
            <TextBlock Text="Contraseña"/>
            <PasswordBox x:Name="PassBox"/>
          </StackPanel>
          <Button Content="Entrar" Click="OnLogin"/>
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</Window>
